# Dashboard — moved

This was a stale copy describing a pre-DuckDB SQLite `buy_list` app that no
longer exists. The current dashboard (DuckDB slim-DB + R2 sync, Streamlit
multi-page) is documented in one canonical place:

➡️ **[docs/modules/dashboard.md](../../dashboard.md)** — module passport
(structure, data dependency, design).

Related:
- **[docs/architecture/local_vs_remote_db.md](../../../architecture/local_vs_remote_db.md)** — the local-full → slim → remote parity contract + audited row counts.
